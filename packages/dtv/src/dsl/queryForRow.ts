// Row-scoped query builder with typed nested selection sets.
// Implementation lives in @kronor/hasura-graphql.
export { queryForRowType } from '@kronor/hasura-graphql';
export type { QueryBuilder } from '@kronor/hasura-graphql';

export { queryForRowType as default } from '@kronor/hasura-graphql';

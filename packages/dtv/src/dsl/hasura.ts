// Typed Hasura filter-expression builder, scoped to a Row shape.
// Implementation lives in @kronor/hasura-graphql.
export { hasuraDSLforRowType } from '@kronor/hasura-graphql';
export type { HasuraForRow } from '@kronor/hasura-graphql';

export { hasuraDSLforRowType as default } from '@kronor/hasura-graphql';
